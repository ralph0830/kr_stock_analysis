# 서버 접속 가이드

## 🌐 외부 접속 주소

### 프론트엔드 (웹 인터페이스)
- **URL**: http://112.219.120.75:5110
- **기능**:
  - 메인 페이지 (/)
  - 대시보드 (/dashboard) - Market Gate 상태, VCP 시그널, 실시간 가격
  - 시그널 페이지 (/signals) - 종가베팅 V2 시그널

### API Gateway (FastAPI)
- **API URL**: http://112.219.120.75:5111
- **Swagger 문서**: http://112.219.120.75:5111/docs
- **ReDoc 문서**: http://112.219.120.75:5111/redoc

### WebSocket
- **WebSocket URL**: ws://112.219.120.75:5111/ws
- **통계**: http://112.219.120.75:5111/ws/stats

## 📡 서비스 포트

| 서비스 | 포트 | 외부 접속 |
|--------|------|-----------|
| Frontend (Next.js) | 5110 | http://112.219.120.75:5110 |
| API Gateway | 5111 | http://112.219.120.75:5111 |
| VCP Scanner | 5112 | http://112.219.120.75:5112 |
| Signal Engine | 5113 | http://112.219.120.75:5113 |
| WebSocket | 5111/ws | ws://112.219.120.75:5111/ws |

## 🔧 서버 관리

### 서비스 시작
```bash
./scripts/start_all.sh
```

### 서비스 중지
```bash
./scripts/stop_all.sh
```

### 로그 확인
```bash
# API Gateway 로그
tail -f logs/api-gateway.log

# Frontend 로그
tail -f logs/frontend.log

# VCP Scanner 로그
tail -f logs/vcp-scanner.log
```

### API 테스트
```bash
# 전체 API 테스트
uv run scripts/test_api.py

# 개별 테스트
curl http://112.219.120.75:5111/health
curl http://112.219.120.75:5111/api/kr/market-gate
```

## 📱 외부 접속 시 주의사항

1. **방화벽**: 포트 5110, 5111, 5112, 5113이 외부에서 접근 가능해야 합니다
2. **네트워크**: 서버 IP가 변경될 경우 위 주소도 업데이트해야 합니다
3. **보안**: 프로덕션 환경에서는 HTTPS와 인증을 구현해야 합니다

## 🧪 API 테스트 결과

### 성공 (5/7)
- ✅ Health Check
- ✅ Market Gate 상태
- ✅ 종가베팅 V2 시그널
- ✅ 실시간 가격 조회
- ✅ WebSocket 통계

### 실패 (2/7)
- ❌ VCP 시그널 목록 (503 - VCP Scanner 서비스 연결 문제)
- ❌ 시스템 메트릭 (응답 형식 불일치)

## 📊 현재 상태

- **인프라**: PostgreSQL, Redis 실행 중
- **백엔드**: API Gateway, VCP Scanner, Signal Engine 실행 중
- **프론트엔드**: Next.js 개발 서버 실행 중
- **외부 접속**: 가능 (0.0.0.0 바인딩)

## 🔄 로컬 접속 (같은 네트워크)

서버와 같은 네트워크에 있는 경우:

```bash
# Frontend
http://112.219.120.75:5110

# API Gateway
http://112.219.120.75:5111
```

## 🌐 인터넷 접속 (공개 IP)

공개 IP를 통해 인터넷에서 접속하려면:

1. **공유기 포트 포워딩 설정**
   - 5110 → 5110 (Frontend)
   - 5111 → 5111 (API Gateway)
   - 5112 → 5112 (VCP Scanner)
   - 5113 → 5113 (Signal Engine)

2. **방화벽 규칙 추가**
```bash
# Ubuntu/Debian
sudo ufw allow 5110/tcp
sudo ufw allow 5111/tcp
sudo ufw allow 5112/tcp
sudo ufw allow 5113/tcp
```

3. **DDNS 설정** (선택)
   - 동적 IP의 경우 DDNS 서비스 사용

## 📞 문제 해결

### 포트 이미 사용 중
```bash
# 사용 중인 프로세스 확인
lsof -i :5110
lsof -i :5111

# 강제 종료
kill -9 <PID>
```

### 외부 접속 안 됨
```bash
# 방화벽 상태 확인
sudo iptables -L -n

# 포트 리스닝 확인
netstat -tuln | grep -E ":(5110|5111|5112|5113)"
```

### 로그 확인
```bash
# 에러 로그
tail -100 logs/*.log

# 실시간 로그
tail -f logs/api-gateway.log
```
