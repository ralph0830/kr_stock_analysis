# VCP Scanner Service
# Port: 5112

services:
  vcp-scanner:
    build:
      context: ../../..
      dockerfile: services/vcp_scanner/Dockerfile
      target: development
    image: vcp-scanner:dev
    container_name: vcp-scanner
    restart: unless-stopped
    ports:
      - "5112:5112"
    environment:
      TZ: Asia/Seoul
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/ralph_stock
      REDIS_URL: redis://redis:6379/0
    volumes:
      - /home/ralph/work/python/kr_stock_analysis/services/vcp_scanner:/app/services/vcp_scanner
      - /home/ralph/work/python/kr_stock_analysis/src:/app/src
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5112/health"]
      interval: 30s
      timeout: 10s
      start_period: 40s
      retries: 3
    networks:
      - ralph-network
